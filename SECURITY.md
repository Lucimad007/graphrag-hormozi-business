# Security

- Never commit `.env`, API keys, or ingested corpora.
- Default Neo4j password `changeme` is for local Docker only.
- Report issues via GitHub; do not open PRs that include secrets or copyrighted source files.
